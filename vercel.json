{
  "buildCommand": "pnpm install --frozen-lockfile && cd packages/widget && pnpm install --frozen-lockfile && pnpm build && cd ../.. && pnpm build",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs"
}
